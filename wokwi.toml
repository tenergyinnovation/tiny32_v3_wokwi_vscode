[wokwi]
version = 1
elf = ".pio/build/nodemcu-32s/firmware.elf"
firmware = ".pio/build/nodemcu-32s/firmware.bin"

# Forward http://localhost:8180 to port 80 on the simulated ESP32:
[[net.forward]]
from = "localhost:8180"
to = "target:80"
